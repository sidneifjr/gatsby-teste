import React from 'react';
import NavMenu from '../components/navMenu';

const Header = () => {
    return (
        <header id="footer">
            <h1>Header</h1>
            <NavMenu />
        </header>
    )
}

export default Header;